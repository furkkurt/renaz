---
import whatsappIcon from '../assets/whatsapp.svg';
import phoneIcon from '../assets/phone.svg';
import bgImage from '../assets/bgImages/bg1.webp';
---

<section class="relative w-full h-screen overflow-hidden">
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0">
    <img
      src={bgImage.src}
      alt="Renaz Teknik Background"
      class="w-full h-full object-cover blur-xs md:blur-sm"
      fetchpriority="high"
      decoding="async"
    />
    <!-- Black Overlay -->
    <div class="absolute inset-0 bg-black/50"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 h-full flex items-center">
    <div class="max-w-7xl mx-auto w-full px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl text-left hero-content-left">
        <!-- H1 Title -->
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-white mb-6 leading-tight" style="font-family: 'Roboto Condensed', sans-serif;">
          Samsun Acil Sıhhi Tesisat & Kombi Servisi
        </h1>
        
        <!-- Yellow Accent Line -->
        <div class="h-1 w-24 bg-[#fad000] mb-6"></div>
        
        <!-- Short Description -->
        <p class="text-lg sm:text-xl lg:text-2xl text-white leading-relaxed mb-8 max-w-2xl" style="font-family: 'Roboto Condensed', sans-serif;">
          Tıkanıklık açma, su kaçağı tespiti, kombi arıza ve petek temizliği hizmetlerini Samsun genelinde hızlı ve garantili şekilde sunuyoruz.
        </p>

        <!-- Buttons -->
        <div class="flex flex-col sm:flex-row items-start gap-4">
          <!-- Phone Button -->
          <a
            href="tel:05346706294"
            class="bg-[#25D366] hover:bg-[#20BA5A] rounded-lg px-6 py-3 shadow-lg hover:shadow-xl transition-all duration-300 flex items-center gap-2 text-white font-semibold text-lg"
            aria-label="Hemen Ara"
          >
            <img
              src={phoneIcon.src}
              alt="Telefon"
              class="w-6 h-6 brightness-0 invert"
            />
            <span>Hemen Ara</span>
          </a>

          <!-- WhatsApp Button -->
          <a
            href="https://wa.me/905346706294"
            target="_blank"
            rel="noopener noreferrer"
            class="bg-[#25D366] hover:bg-[#20BA5A] rounded-lg px-6 py-3 shadow-lg hover:shadow-xl transition-all duration-300 flex items-center gap-2 text-white font-semibold text-lg"
            aria-label="WhatsApp'tan Yaz"
          >
            <img
              src={whatsappIcon.src}
              alt="WhatsApp"
              class="w-6 h-6 brightness-0 invert"
            />
            <span>WhatsApp'tan Yaz</span>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

